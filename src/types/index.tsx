export interface NavItem {
	name: string;
	url: string;
}

export interface Tutorial {
	title: string;
	slug: string;
	content: string;
}

export interface FAQ {
	title: string;
	content: string;
}

export interface AccordionItem {
	title: string;
	content: string;
}